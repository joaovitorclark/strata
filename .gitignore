node_modules
dist
dist-win/
.cache/
.DS_Store
*.local
data/
cypress/reports/
cypress/screenshots/
cypress/videos/
# `data/` matches ANY directory named data, including cypress/fixtures/data/.
# Negations must come after that rule so the committed fixtures are tracked.
!cypress/fixtures/data/
!cypress/fixtures/data/**
