version: 2
models:
  - name: dim_loja
    description: Tabela de negócio dim_loja no domínio varejo.
    config:
      tags:
        - strata:managed
        - strata:vendas
      meta:
        strata:
          managed: true
          transform:
            from:
              ref: loja
              alias: s0
            joins: []
            where: ""
            group_by: []
      schema: gold
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - loja_id
        warn_unsupported: false
    columns:
      - name: loja_id
        data_type: bigint
        config:
          meta:
            strata:
              lineage:
                - from: silver.loja.loja_id
        constraints:
          - type: not_null
            warn_unsupported: false
      - name: nome
        data_type: string
        config:
          meta:
            strata:
              lineage:
                - from: silver.loja.nome
      - name: uf
        data_type: string
        config:
          meta:
            strata:
              lineage:
                - from: silver.loja.uf
      - name: cidade
        data_type: string
        config:
          meta:
            strata:
              lineage:
                - from: silver.loja.cidade
