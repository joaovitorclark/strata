name: strata
version: 1.0.0
config-version: 2
profile: strata
model-paths:
  - models
seed-paths:
  - seeds
clean-targets:
  - target
  - dbt_packages
dispatch:
  - macro_namespace: dbt_utils
    search_order:
      - strata
