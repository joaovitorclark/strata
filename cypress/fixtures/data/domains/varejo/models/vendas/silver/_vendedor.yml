version: 2
models:
  - name: vendedor
    config:
      tags:
        - strata:managed
        - strata:vendas
      schema: silver
      contract:
        enforced: true
      meta:
        strata:
          managed: true
    constraints:
      - type: primary_key
        columns:
          - vendedor_id
        warn_unsupported: false
    columns:
      - name: vendedor_id
        data_type: bigint
        config:
          meta:
            strata:
              lineage:
                - from: bronze.vendedores.vendedor_id
        constraints:
          - type: not_null
            warn_unsupported: false
      - name: nome
        data_type: string
        config:
          meta:
            strata:
              lineage:
                - from: bronze.vendedores.nome
        constraints:
          - type: not_null
            warn_unsupported: false
      - name: loja_id
        data_type: bigint
        config:
          meta:
            strata:
              lineage:
                - from: bronze.vendedores.loja_id
      - name: ativo
        data_type: boolean
        config:
          meta:
            strata:
              lineage:
                - from: bronze.vendedores.ativo
