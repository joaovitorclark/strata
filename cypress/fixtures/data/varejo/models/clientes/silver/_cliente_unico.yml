version: 2
models:
  - name: cliente_unico
    config:
      tags:
        - strata:clientes
      meta:
        strata:
          table_id: silver.cliente_unico
          layer: silver
          schema: silver
          pk:
            - cliente_sk
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - cliente_sk
        warn_unsupported: false
    columns:
      - name: cliente_sk
        data_type: string
        config:
          meta:
            strata:
              not_null: true
        constraints:
          - type: not_null
            warn_unsupported: false
      - name: cliente_id
        data_type: bigint
        config:
          meta:
            strata:
              not_null: true
        constraints:
          - type: not_null
            warn_unsupported: false
      - name: email
        data_type: string
